exports.func = function() {
  /// bubble sort

  function bubbleSort(arr) {
    return arr;
  }

  // Write Javascript code!
  var result = bubbleSort([3, 2, 4, 5]);
  const appDiv = document.getElementById('app');
  appDiv.innerHTML = `${JSON.stringify(result)}`;
};
